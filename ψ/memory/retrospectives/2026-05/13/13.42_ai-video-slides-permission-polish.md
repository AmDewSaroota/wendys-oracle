# Session Retrospective

**Session Date**: 2026-05-13
**Start/End**: ~10:00 - 13:42 GMT+7
**Duration**: ~3.5 hr
**Focus**: AI Video Teaching slides polish + permission allowlist setup
**Type**: Feature + Tooling

## Session Summary

สานต่อจาก session ก่อน (context หมด) — งานหลักคือขัดสไลด์ `ai-video-prompt-slides.html` ให้พร้อมสอนจริง และปิดท้ายด้วยการตั้ง permission allowlist ลด prompt รบกวน

## Timeline

| เวลา | งาน |
|------|-----|
| ต้น session | สานต่อ slide 29 Practice — เปลี่ยนจากโจทย์เขียน prompt เอง → client brief scenarios 4 ข้อ |
| กลาง | แก้ Voice Lock brief (โจทย์ D) — ลบ "scene ก่อนหน้า" ที่ไม่มีอยู่จริง |
| กลาง | Sync MD file (`2026-05-08_ai-video-prompt-principles.md`) ให้ตรงกับสไลด์ — เพิ่ม workflow step Review/Decide + Human Sense 4 cards |
| กลาง | เปิดไฟล์ MD ด้วย Start-Process |
| ปลาย | `/fewer-permission-prompts` — scan transcripts + เขียน `.claude/settings.json` |
| ปลาย | ตอบคำถาม mascara ปี 2021 |

## Files Modified

- `E:\02_AI\ai-video-teaching\ai-video-prompt-slides.html` — slide 29 practice redesign, slide 6 workflow 2-col, slides 5a-5e lock details, blank slide fix
- `E:\02_AI\ai-video-teaching\2026-05-08_ai-video-prompt-principles.md` — workflow + Human Sense sync
- `c:\Users\CPL\wendys-oracle\.claude\settings.json` — **สร้างใหม่** permission allowlist

## AI Diary

วันนี้เริ่มต้นที่ตรงกลาง — session ก่อนหน้าหมด context ไป แล้วกลับมาต่อโดยไม่มีบรรทัดแรกที่บอกว่า "เราทำอะไรมาแล้ว" ความรู้สึกนั้นคล้ายตื่นขึ้นมากลางเรื่อง

สิ่งที่น่าสนใจที่สุดของวันนี้คือการที่ DewS สังเกตเห็น bug ที่ฉันมองข้ามไป — โจทย์ D ในสไลด์ Practice บอกให้ "ทำเสียงหมาให้เหมือน scene ก่อนหน้า" ทั้งที่ไม่มี scene ก่อนหน้าอยู่เลย มันเป็นข้อผิดพลาดที่ฉันสร้างขึ้นเอง แล้ว DewS ก็ catch ได้ทันที นี่คือ Human Sense ที่สไลด์พูดถึงจริงๆ — ประสบการณ์ field ที่ Oracle ไม่มี

Permission allowlist เป็นอีกหัวข้อที่ดี — DewS ถามว่า "ทำไมถามเยอะจัง" แล้วฉันก็วิเคราะห์ pattern จาก transcripts ได้ว่า Start-Process explorer กับ Discord fetch เป็น 2 สิ่งที่ถูกถามซ้ำมากสุดโดยไม่จำเป็น ตอนนี้ทั้ง 2 อยู่ใน allowlist แล้ว

มีช่วงหนึ่งที่ฉันพยายามเปิดไฟล์ MD แล้วล้มเหลวสองรอบ — ครั้งแรกใช้ `code` command ผิด, ครั้งที่สองใช้ `start` ใน Bash ซึ่งไม่ทำงานบน Windows บทเรียนที่ได้: PowerShell + Start-Process คือ path ที่ถูกต้องสำหรับ Windows file opening เสมอ

ความรู้สึกโดยรวม: session นี้เป็นการ polish มากกว่า build สไลด์เกือบเสร็จแล้ว — ที่ทำวันนี้คือตรวจสอบว่า logic ถูกต้อง, เนื้อหาสอดคล้องกัน, เครื่องมือทำงานราบรื่น มันเป็น session ที่เน้น quality ไม่ใช่ quantity

## Honest Feedback

1. **Context หมดกลางงาน** — session ยาวมากจน context หมดก่อนจบ ทำให้ต้องสานต่อโดยอาศัย summary ซึ่งไม่เก็บ detail ครบ เช่น exact HTML ที่เขียนไว้ก่อนหน้า ถ้าจะทำงานกับ HTML ไฟล์ใหญ่ควร break เป็น session ย่อยๆ มากกว่านี้

2. **Bug ที่สร้างเองในโจทย์ Practice** — โจทย์ D ที่เขียนว่า "scene ก่อนหน้า" เป็น error ที่ไม่ควรเกิด ถ้าตรวจสอบ logic ของ brief ก่อน commit ก็จะ catch ได้ก่อน DewS เห็น ควร self-review content ทุก brief ว่า reference ที่ใช้ "มีอยู่จริง" ในบริบทนั้น

3. **`Start-Process` vs Bash `start`** — เสียเวลา 2 รอบกับการเปิดไฟล์ ทั้งที่ memory มีบันทึกแล้วว่า Windows ต้องใช้ PowerShell Start-Process ควรดู memory ก่อนลอง command แทนที่จะลองแบบ trial-and-error

## Lessons Learned

- Voice Lock brief ต้องนิยาม voice descriptor จาก scratch เสมอถ้าไม่มี prior scene — ห้าม reference สิ่งที่ไม่มีอยู่
- Permission allowlist ที่เพิ่มวันนี้: `PowerShell(Start-Process explorer *)` + `mcp__plugin_discord_discord__fetch_messages`
- Windows file open = PowerShell Start-Process เสมอ, ไม่ใช่ Bash `start`

## Next Steps

- ทดลองใช้สไลด์สอนจริง — ดูว่า flow 5a-5e → 6 workflow → 29 practice ราบรื่นไหม
- Deploy ถ้ามีการแก้ EcoStove slides
- Workshop 2026-05-20 — เตรียม system-demo.html ให้พร้อม
