# Handoff: Field Test — 7 Bugs from DewS

**Date**: 2026-03-26 23:50
**Context**: 99%

## What We Did (This Session)

- **Session History sub-tab** — ย้ายจาก main tab → sub-tab ใน "ข้อมูล" (sensor/tvoc/sessions)
- **Collection period warning** — Overview: rose banner (auto-hide เมื่อตั้งครบ) + Schedule: checklist ❌/✅
- **Overview card colors** — เปลี่ยนจาก amber ซ้ำ → emerald/blue/amber/purple
- **Hero no-old card** — ย่อข้อความ + คืน outline box
- **Guide threshold** — เพิ่มคำอธิบาย ≥22 readings = สมบูรณ์
- **DB fix** — DewS รัน SQL: UPDATE sessions SET complete WHERE readings_count >= 22

## Field Test Context

DewS เอาเซนเซอร์ 4 ตัว (BS 001, 005, 010, 011) ออกไปทดสอบภาคสนามจริง:
- 001, 005 ติด cooldown ตอนแรก → รอจนหมด cooldown ค่อยเชื่อมต่อ
- 010 แบตหมด → ทิ้งชาร์จที่บ้าน (ยังต่อ hotspot อยู่ = เก็บข้อมูลต่อ)
- 011 เริ่มเก็บก่อน ออกไปร้านอาหาร
- DewS ใช้มือถือเครื่องเดียวสลับดูทุกเซนเซอร์ (หลายแท็บ)
- ตั้ง GPS ร้านอาหาร เพื่อทดสอบ GPS lock
- กด manual cut Ambient ให้ 005 + 011, ปล่อย 001 ให้ auto-cut

## Pending — 7 Bugs to Fix

### Bug 1: GPS auto-lock ข้ามหน้ายืนยัน (volunteer.html)
- **อาการ**: พอ GPS ตรงกับบ้าน ระบบเริ่ม Baseline ทันทีไม่รอกด
- **ที่ควรเป็น**: หน้ายืนยันมีอยู่แล้ว แต่ skip เร็วเกิน → ต้องหยุดรอจนกว่าอาสากดยืนยัน
- **ไฟล์**: `lab/tuya-ecostove/deploy/volunteer.html`

### Bug 2: Popup กรอกมือโผล่ซ้ำหลัง refresh (volunteer.html)
- **อาการ**: กด "ไม่เก็บ" ไปแล้ว แต่พอ refresh + เลือกบ้านใหม่ popup ถามอีก (ไม่ consistent)
- **สาเหตุน่าจะ**: session state ไม่ persist ข้าม refresh หรือ logic เช็ค flag ผิด
- **ไฟล์**: `lab/tuya-ecostove/deploy/volunteer.html`
- **Note**: เคยแก้ไปแล้วใน commit aa79020 แต่ยังเกิดอยู่

### Bug 3: Baseline logic ผิด 2 กรณี (sync.js + volunteer.html)
- **010 (ทิ้งที่บ้าน)**: เก็บค่าเกือบเต็มแต่ยังไม่ auto-cut — ยังโชว์ปุ่ม "เริ่มจุดเตา"
- **001 (ปล่อย auto-cut)**: หน้าอนุมัติขึ้น "ไม่มีข้อมูล Ambient" ทั้งที่ควรมี
- **ต้องตรวจ**: auto-cut logic ใน sync.js (baseline_transition), volunteer.html state management
- **ไฟล์**: `lab/tuya-ecostove/deploy/api/sync.js`, `lab/tuya-ecostove/deploy/volunteer.html`

### Bug 4: ข้อความ quiet hours กำกวม (volunteer.html)
- **อาการ**: ตอน 2 ทุ่ม ขึ้นว่า "อยู่ในช่วงหยุดเก็บข้อมูล (00:00-06:00)" ทั้งที่ยังไม่ถึงเที่ยงคืน
- **จริงๆ คือ**: session 1 จบ + cooldown 5 ชม. → session 2 ตกช่วง quiet hours → เก็บไม่ได้ต้องรอพรุ่งนี้
- **ที่ควรเป็น**: ข้อความอธิบายเหตุผลจริง เช่น "ครบ 1 รอบแล้ว — รอบถัดไปอยู่นอกเวลาเก็บข้อมูล เก็บต่อได้พรุ่งนี้"
- **ไฟล์**: `lab/tuya-ecostove/deploy/volunteer.html`

### Bug 5: ข้อความ "เก็บเสร็จ" กับ "ยังเก็บอยู่" ขัดกัน (volunteer.html)
- **อาการ**: กรอบเขียว: "Session 1 เสร็จแล้ว" + กรอบด้านล่าง: "เซนเซอร์ยังเก็บข้อมูลอยู่"
- **ที่ควรเป็น**: ถ้า session จบแล้ว กรอบ "ยังเก็บอยู่" ต้องหายไป
- **ไฟล์**: `lab/tuya-ecostove/deploy/volunteer.html`

### Bug 6: ไม่มีหน้าสรุปหลัง session จบ (volunteer.html)
- **อาการ**: เปิดมาเจอจอ quiet hours ทันที ไม่มีสรุปว่าเก็บได้กี่ค่า
- **ที่ควรเป็น**: session จบ → แสดงสรุป (กี่ค่า, กี่นาที, PM2.5 avg) → อาสากดรับทราบ → ค่อยเข้าจอ idle
- **ไฟล์**: `lab/tuya-ecostove/deploy/volunteer.html`

### Bug 7: Session 010 โดนแยก 2 การ์ด (index.html approval)
- **อาการ**: เครื่องเดียวแต่ approval แยก 2 การ์ด (18:15-18:20 · 2 logs + 20:05-20:25 · 5 logs)
- **สาเหตุ**: Tuya Cloud ขัดข้อง → gap > 30 นาที → grouping logic แยกเป็น 2 sessions
- **ต้องคิด**: ควรเพิ่ม tolerance สำหรับ Tuya gaps? หรือ merge cards ที่มา session_id เดียวกัน?
- **ไฟล์**: `lab/tuya-ecostove/deploy/index.html` (loadAdminData grouping logic)

## Key Files

- `lab/tuya-ecostove/deploy/volunteer.html` — หน้าอาสาสมัคร (Bug 1-6)
- `lab/tuya-ecostove/deploy/api/sync.js` — Sync + session logic (Bug 3)
- `lab/tuya-ecostove/deploy/index.html` — Dashboard + approval grouping (Bug 7)

## Git Status

- Working tree clean
- 4 commits ahead of origin (unpushed)
