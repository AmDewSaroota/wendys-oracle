# Mosquitto config for Gemini Proxy
# TCP listener for CLI/Node scripts
listener 1883
protocol mqtt

# WebSocket listener for browser extension
listener 9001
protocol websockets

# Allow anonymous connections (local only)
allow_anonymous true
